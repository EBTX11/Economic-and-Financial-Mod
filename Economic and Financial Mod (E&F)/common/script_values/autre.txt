country_reserves = {
    every_country = {
        limit = { gdp = root }
        add = gold_reserves
    }

    is_shown = {
        any_country = {
            gdp = scope:saved_gdp
            gold_reserves > 1000000
        }
    } 
}