#ef_economy_1_formwork
type money_value_in_gold_top_bar = flowcontainer = {

	textbox = {
		parentanchor = vcenter
		text = "money_value_in_gold_top_bar"
		align = left|nobaseline
		autoresize = yes
	}
}

# #national_currency_distribution
# type national_currency_distribution = flowcontainer {
	
# 	#algerian_dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		#visible = "[GetScriptedGui('law_algerian_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_algerian_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('algerian_dinar_c_global_stokpile')|+D] @algerian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_algerian_dinar_state')|+D] @algerian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('algerian_dinar_c_no_own')|+D] @algerian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#argentine_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_argentine_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_argentine_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('argentine_peso_c_global_stokpile')|+D] @argentine_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_argentine_peso_state')|+D] @argentine_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('argentine_peso_c_no_own')|+D] @argentine_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ariary
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ariary_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ariary_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ariary_c_global_stokpile')|+D] @ariary_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ariary_state')|+D] @ariary_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ariary_c_no_own')|+D] @ariary_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#australian_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_australian_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_australian_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('australian_dollar_c_global_stokpile')|+D] @australian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_australian_dollar_state')|+D] @australian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('australian_dollar_c_no_own')|+D] @australian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#baht
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_baht_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_baht_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('baht_c_global_stokpile')|+D] @baht_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_baht_state')|+D] @baht_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('baht_c_no_own')|+D] @baht_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#bavarian_gulden
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_bavarian_gulden_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_bavarian_gulden_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('bavarian_gulden_c_global_stokpile')|+D] @bavarian_gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_bavarian_gulden_state')|+D] @bavarian_gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('bavarian_gulden_c_no_own')|+D] @bavarian_gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#belgian_franc
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_belgian_franc_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_belgian_franc_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('belgian_franc_c_global_stokpile')|+D] @belgian_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_belgian_franc_state')|+D] @belgian_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('belgian_franc_c_no_own')|+D] @belgian_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#brazilian_real
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_brazilian_real_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_brazilian_real_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('brazilian_real_c_global_stokpile')|+D] @brazilian_real_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_brazilian_real_state')|+D] @brazilian_real_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('brazilian_real_c_no_own')|+D] @brazilian_real_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#canadian_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_canadian_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_canadian_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('canadian_dollar_c_global_stokpile')|+D] @canadian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_canadian_dollar_state')|+D] @canadian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('canadian_dollar_c_no_own')|+D] @canadian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#caribbean_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_caribbean_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_caribbean_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('caribbean_dollar_c_global_stokpile')|+D] @caribbean_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_caribbean_dollar_state')|+D] @caribbean_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('caribbean_dollar_c_no_own')|+D] @caribbean_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#central_african_eco
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_central_african_eco_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_central_african_eco_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('central_african_eco_c_global_stokpile')|+D] @central_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_central_african_eco_state')|+D] @central_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('central_african_eco_c_no_own')|+D] @central_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#chilean_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_chilean_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_chilean_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('chilean_peso_c_global_stokpile')|+D] @chilean_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_chilean_peso_state')|+D] @chilean_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('chilean_peso_c_no_own')|+D] @chilean_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#colombian_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_colombian_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_colombian_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('colombian_peso_c_global_stokpile')|+D] @colombian_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_colombian_peso_state')|+D] @colombian_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('colombian_peso_c_no_own')|+D] @colombian_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#confederate_states_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_confederate_states_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_confederate_states_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('confederate_states_dollar_c_global_stokpile')|+D] @confederate_states_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_confederate_states_dollar_state')|+D] @confederate_states_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('confederate_states_dollar_c_no_own')|+D] @confederate_states_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#costa_rican_colon
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_costa_rican_colon_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_costa_rican_colon_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('costa_rican_colon_c_global_stokpile')|+D] @costa_rican_colon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_costa_rican_colon_state')|+D] @costa_rican_colon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('costa_rican_colon_c_no_own')|+D] @costa_rican_colon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#cuban_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_cuban_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_cuban_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('cuban_peso_c_global_stokpile')|+D] @cuban_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_cuban_peso_state')|+D] @cuban_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('cuban_peso_c_no_own')|+D] @cuban_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#czech_koruna
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_czech_koruna_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_czech_koruna_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('czech_koruna_c_global_stokpile')|+D] @czech_koruna_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_czech_koruna_state')|+D] @czech_koruna_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('czech_koruna_c_no_own')|+D] @czech_koruna_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#danish_krone
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_danish_krone_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_danish_krone_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('danish_krone_c_global_stokpile')|+D] @danish_krone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_danish_krone_state')|+D] @danish_krone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('danish_krone_c_no_own')|+D] @danish_krone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#dong
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_dong_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_dong_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('dong_c_global_stokpile')|+D] @dong_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_dong_state')|+D] @dong_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('dong_c_no_own')|+D] @dong_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#drachma
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_drachma_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_drachma_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('drachma_c_global_stokpile')|+D] @drachma_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_drachma_state')|+D] @drachma_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('drachma_c_no_own')|+D] @drachma_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('dinar_c_global_stokpile')|+D] @dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_dinar_state')|+D] @dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('dinar_c_no_own')|+D] @dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ducato
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ducato_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ducato_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ducato_c_global_stokpile')|+D] @ducato_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ducato_state')|+D] @ducato_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ducato_c_no_own')|+D] @ducato_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#east_african_eco
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_east_african_eco_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_east_african_eco_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('east_african_eco_c_global_stokpile')|+D] @east_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_east_african_eco_state')|+D] @east_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('east_african_eco_c_no_own')|+D] @east_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ecuadorian_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ecuadorian_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ecuadorian_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ecuadorian_peso_c_global_stokpile')|+D] @ecuadorian_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ecuadorian_peso_state')|+D] @ecuadorian_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ecuadorian_peso_c_no_own')|+D] @ecuadorian_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#egyptian_pound
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_egyptian_pound_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_egyptian_pound_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('egyptian_pound_c_global_stokpile')|+D] @egyptian_pound_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_egyptian_pound_state')|+D] @egyptian_pound_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('egyptian_pound_c_no_own')|+D] @egyptian_pound_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#el_salvador_colon
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_el_salvador_colon_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_el_salvador_colon_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('el_salvador_colon_c_global_stokpile')|+D] @el_salvador_colon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_el_salvador_colon_state')|+D] @el_salvador_colon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('el_salvador_colon_c_no_own')|+D] @el_salvador_colon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#estonian_kroon
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_estonian_kroon_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_estonian_kroon_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('estonian_kroon_c_global_stokpile')|+D] @estonian_kroon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_estonian_kroon_state')|+D] @estonian_kroon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('estonian_kroon_c_no_own')|+D] @estonian_kroon_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ethiopian_birr
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ethiopian_birr_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ethiopian_birr_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ethiopian_birr_c_global_stokpile')|+D] @ethiopian_birr_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ethiopian_birr_state')|+D] @ethiopian_birr_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ethiopian_birr_c_no_own')|+D] @ethiopian_birr_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#finnish_markka
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_finnish_markka_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_finnish_markka_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('finnish_markka_c_global_stokpile')|+D] @finnish_markka_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_finnish_markka_state')|+D] @finnish_markka_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('finnish_markka_c_no_own')|+D] @finnish_markka_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#florin
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_florin_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_florin_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('florin_c_global_stokpile')|+D] @florin_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_florin_state')|+D] @florin_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('florin_c_no_own')|+D] @florin_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#french_franc
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_french_franc_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_french_franc_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('french_franc_c_global_stokpile')|+D] @french_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_french_franc_state')|+D] @french_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('french_franc_c_no_own')|+D] @french_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ghanaian_cedi
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ghanaian_cedi_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ghanaian_cedi_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ghanaian_cedi_c_global_stokpile')|+D] @ghanaian_cedi_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ghanaian_cedi_state')|+D] @ghanaian_cedi_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ghanaian_cedi_c_no_own')|+D] @ghanaian_cedi_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#gulden
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_gulden_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_gulden_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('gulden_c_global_stokpile')|+D] @gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_gulden_state')|+D] @gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('gulden_c_no_own')|+D] @gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#guatemalan_quetzal
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_guatemalan_quetzal_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_guatemalan_quetzal_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('guatemalan_quetzal_c_global_stokpile')|+D] @guatemalan_quetzal_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_guatemalan_quetzal_state')|+D] @guatemalan_quetzal_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('guatemalan_quetzal_c_no_own')|+D] @guatemalan_quetzal_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#hannoveraner_thaler
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_hannoveraner_thaler_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_hannoveraner_thaler_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('hannoveraner_thaler_c_global_stokpile')|+D] @hannoveraner_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_hannoveraner_thaler_state')|+D] @hannoveraner_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('hannoveraner_thaler_c_no_own')|+D] @hannoveraner_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#honduran_lempira
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_honduran_lempira_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_honduran_lempira_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('honduran_lempira_c_global_stokpile')|+D] @honduran_lempira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_honduran_lempira_state')|+D] @honduran_lempira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('honduran_lempira_c_no_own')|+D] @honduran_lempira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#hungarian_forint
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_hungarian_forint_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_hungarian_forint_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('hungarian_forint_c_global_stokpile')|+D] @hungarian_forint_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_hungarian_forint_state')|+D] @hungarian_forint_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('hungarian_forint_c_no_own')|+D] @hungarian_forint_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#icelandic_krona
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_icelandic_krona_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_icelandic_krona_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('icelandic_krona_c_global_stokpile')|+D] @icelandic_krona_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_icelandic_krona_state')|+D] @icelandic_krona_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('icelandic_krona_c_no_own')|+D] @icelandic_krona_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#indian_rupee
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_indian_rupee_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_indian_rupee_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('indian_rupee_c_global_stokpile')|+D] @indian_rupee_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_indian_rupee_state')|+D] @indian_rupee_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('indian_rupee_c_no_own')|+D] @indian_rupee_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#indies_guilder
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_indies_guilder_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_indies_guilder_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('indies_guilder_c_global_stokpile')|+D] @indies_guilder_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_indies_guilder_state')|+D] @indies_guilder_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('indies_guilder_c_no_own')|+D] @indies_guilder_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#indonesian_rupiah
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_indonesian_rupiah_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_indonesian_rupiah_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('indonesian_rupiah_c_global_stokpile')|+D] @indonesian_rupiah_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_indonesian_rupiah_state')|+D] @indonesian_rupiah_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('indonesian_rupiah_c_no_own')|+D] @indonesian_rupiah_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#iraqi_dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_iraqi_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_iraqi_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('iraqi_dinar_c_global_stokpile')|+D] @iraqi_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_iraqi_dinar_state')|+D] @iraqi_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('iraqi_dinar_c_no_own')|+D] @iraqi_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#irish_pound
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_irish_pound_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_irish_pound_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('irish_pound_c_global_stokpile')|+D] @irish_pound_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_irish_pound_state')|+D] @irish_pound_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('irish_pound_c_no_own')|+D] @irish_pound_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#korean_won
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_korean_won_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_korean_won_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('korean_won_c_global_stokpile')|+D] @korean_won_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_korean_won_state')|+D] @korean_won_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('korean_won_c_no_own')|+D] @korean_won_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#laotian_kip
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_laotian_kip_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_laotian_kip_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('laotian_kip_c_global_stokpile')|+D] @laotian_kip_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_laotian_kip_state')|+D] @laotian_kip_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('laotian_kip_c_no_own')|+D] @laotian_kip_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#latvian_lats
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_latvian_lats_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_latvian_lats_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('latvian_lats_c_global_stokpile')|+D] @latvian_lats_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_latvian_lats_state')|+D] @latvian_lats_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('latvian_lats_c_no_own')|+D] @latvian_lats_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#leu
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_leu_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_leu_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('leu_c_global_stokpile')|+D] @leu_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_leu_state')|+D] @leu_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('leu_c_no_own')|+D] @leu_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#lev
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_lev_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_lev_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('lev_c_global_stokpile')|+D] @lev_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_lev_state')|+D] @lev_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('lev_c_no_own')|+D] @lev_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#liberian_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_liberian_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_liberian_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('liberian_dollar_c_global_stokpile')|+D] @liberian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_liberian_dollar_state')|+D] @liberian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('liberian_dollar_c_no_own')|+D] @liberian_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#libyan_dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_libyan_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_libyan_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('libyan_dinar_c_global_stokpile')|+D] @libyan_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_libyan_dinar_state')|+D] @libyan_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('libyan_dinar_c_no_own')|+D] @libyan_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#lira
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_lira_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_lira_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('lira_c_global_stokpile')|+D] @lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_lira_state')|+D] @lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('lira_c_no_own')|+D] @lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#lithuanian_litas
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_lithuanian_litas_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_lithuanian_litas_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('lithuanian_litas_c_global_stokpile')|+D] @lithuanian_litas_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_lithuanian_litas_state')|+D] @lithuanian_litas_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('lithuanian_litas_c_no_own')|+D] @lithuanian_litas_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#luxembourgish_franc
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_luxembourgish_franc_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_luxembourgish_franc_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('luxembourgish_franc_c_global_stokpile')|+D] @luxembourgish_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_luxembourgish_franc_state')|+D] @luxembourgish_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('luxembourgish_franc_c_no_own')|+D] @luxembourgish_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#mark
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_mark_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_mark_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('mark_c_global_stokpile')|+D] @mark_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_mark_state')|+D] @mark_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('mark_c_no_own')|+D] @mark_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#mexican_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_mexican_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_mexican_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('mexican_peso_c_global_stokpile')|+D] @mexican_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_mexican_peso_state')|+D] @mexican_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('mexican_peso_c_no_own')|+D] @mexican_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#moroccan_dirham
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_moroccan_dirham_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_moroccan_dirham_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('moroccan_dirham_c_global_stokpile')|+D] @moroccan_dirham_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_moroccan_dirham_state')|+D] @moroccan_dirham_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('moroccan_dirham_c_no_own')|+D] @moroccan_dirham_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#new_zealand_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_new_zealand_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_new_zealand_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('new_zealand_dollar_c_global_stokpile')|+D] @new_zealand_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_new_zealand_dollar_state')|+D] @new_zealand_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('new_zealand_dollar_c_no_own')|+D] @new_zealand_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#nicaraguan_cordoba
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_nicaraguan_cordoba_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_nicaraguan_cordoba_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('nicaraguan_cordoba_c_global_stokpile')|+D] @nicaraguan_cordoba_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_nicaraguan_cordoba_state')|+D] @nicaraguan_cordoba_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('nicaraguan_cordoba_c_no_own')|+D] @nicaraguan_cordoba_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#nigerian_naira
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_nigerian_naira_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_nigerian_naira_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('nigerian_naira_c_global_stokpile')|+D] @nigerian_naira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_nigerian_naira_state')|+D] @nigerian_naira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('nigerian_naira_c_no_own')|+D] @nigerian_naira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#norwegian_krone
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_norwegian_krone_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_norwegian_krone_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('norwegian_krone_c_global_stokpile')|+D] @norwegian_krone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_norwegian_krone_state')|+D] @norwegian_krone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('norwegian_krone_c_no_own')|+D] @norwegian_krone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#omanian_rial
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_omanian_rial_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_omanian_rial_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('omanian_rial_c_global_stokpile')|+D] @omanian_rial_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_omanian_rial_state')|+D] @omanian_rial_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('omanian_rial_c_no_own')|+D] @omanian_rial_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ottoman_lira
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ottoman_lira_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ottoman_lira_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ottoman_lira_c_global_stokpile')|+D] @ottoman_lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ottoman_lira_state')|+D] @ottoman_lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ottoman_lira_c_no_own')|+D] @ottoman_lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#pakistani_rupee
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_pakistani_rupee_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_pakistani_rupee_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('pakistani_rupee_c_global_stokpile')|+D] @pakistani_rupee_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_pakistani_rupee_state')|+D] @pakistani_rupee_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('pakistani_rupee_c_no_own')|+D] @pakistani_rupee_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#paraguayan_guarani
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_paraguayan_guarani_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_paraguayan_guarani_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('paraguayan_guarani_c_global_stokpile')|+D] @paraguayan_guarani_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_paraguayan_guarani_state')|+D] @paraguayan_guarani_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('paraguayan_guarani_c_no_own')|+D] @paraguayan_guarani_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#peseta
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_peseta_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_peseta_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('peseta_c_global_stokpile')|+D] @peseta_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_peseta_state')|+D] @peseta_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('peseta_c_no_own')|+D] @peseta_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('peso_c_global_stokpile')|+D] @peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_peso_state')|+D] @peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('peso_c_no_own')|+D] @peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#peso_bolivien
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_peso_bolivien_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_peso_bolivien_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('peso_bolivien_c_global_stokpile')|+D] @peso_bolivien_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_peso_bolivien_state')|+D] @peso_bolivien_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('peso_bolivien_c_no_own')|+D] @peso_bolivien_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#philippine_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_philippine_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_philippine_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('philippine_peso_c_global_stokpile')|+D] @philippine_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_philippine_peso_state')|+D] @philippine_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('philippine_peso_c_no_own')|+D] @philippine_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#pound_sterling
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_pound_sterling_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_pound_sterling_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('pound_sterling_c_global_stokpile')|+D] @pound_sterling_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_pound_sterling_state')|+D] @pound_sterling_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('pound_sterling_c_no_own')|+D] @pound_sterling_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#prussian_thaler
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_prussian_thaler_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_prussian_thaler_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('prussian_thaler_c_global_stokpile')|+D] @prussian_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_prussian_thaler_state')|+D] @prussian_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('prussian_thaler_c_no_own')|+D] @prussian_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#qiran
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_qiran_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_qiran_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('qiran_c_global_stokpile')|+D] @qiran_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_qiran_state')|+D] @qiran_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('qiran_c_no_own')|+D] @qiran_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#real
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_real_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_real_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('real_c_global_stokpile')|+D] @real_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_real_state')|+D] @real_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('real_c_no_own')|+D] @real_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#ruble
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_ruble_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_ruble_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ruble_c_global_stokpile')|+D] @ruble_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_ruble_state')|+D] @ruble_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('ruble_c_no_own')|+D] @ruble_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#saudi_riyal
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_saudi_riyal_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_saudi_riyal_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('saudi_riyal_c_global_stokpile')|+D] @saudi_riyal_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_saudi_riyal_state')|+D] @saudi_riyal_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('saudi_riyal_c_no_own')|+D] @saudi_riyal_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#saxon_thaler
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_saxon_thaler_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_saxon_thaler_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('saxon_thaler_c_global_stokpile')|+D] @saxon_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_saxon_thaler_state')|+D] @saxon_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('saxon_thaler_c_no_own')|+D] @saxon_thaler_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#scudo_pontificio
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_scudo_pontificio_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_scudo_pontificio_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('scudo_pontificio_c_global_stokpile')|+D] @scudo_pontificio_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_scudo_pontificio_state')|+D] @scudo_pontificio_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('scudo_pontificio_c_no_own')|+D] @scudo_pontificio_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#scudo_sardo
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_scudo_sardo_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_scudo_sardo_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('scudo_sardo_c_global_stokpile')|+D] @scudo_sardo_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_scudo_sardo_state')|+D] @scudo_sardo_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('scudo_sardo_c_no_own')|+D] @scudo_sardo_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#serbian_dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_serbian_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_serbian_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('serbian_dinar_c_global_stokpile')|+D] @serbian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_serbian_dinar_state')|+D] @serbian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('serbian_dinar_c_no_own')|+D] @serbian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#sierra_leonean_leone
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_sierra_leonean_leone_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_sierra_leonean_leone_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('sierra_leonean_leone_c_global_stokpile')|+D] @sierra_leonean_leone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_sierra_leonean_leone_state')|+D] @sierra_leonean_leone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('sierra_leonean_leone_c_no_own')|+D] @sierra_leonean_leone_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#slovak_koruna
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_slovak_koruna_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_slovak_koruna_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('slovak_koruna_c_global_stokpile')|+D] @slovak_koruna_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_slovak_koruna_state')|+D] @slovak_koruna_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('slovak_koruna_c_no_own')|+D] @slovak_koruna_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#sol_de_oro
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_sol_de_oro_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_sol_de_oro_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('sol_de_oro_c_global_stokpile')|+D] @sol_de_oro_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_sol_de_oro_state')|+D] @sol_de_oro_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('sol_de_oro_c_no_own')|+D] @sol_de_oro_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#south_african_rand
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_south_african_rand_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_south_african_rand_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('south_african_rand_c_global_stokpile')|+D] @south_african_rand_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_south_african_rand_state')|+D] @south_african_rand_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('south_african_rand_c_no_own')|+D] @south_african_rand_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#south_german_gulden
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_south_german_gulden_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_south_german_gulden_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('south_german_gulden_c_global_stokpile')|+D] @south_german_gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_south_german_gulden_state')|+D] @south_german_gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('south_german_gulden_c_no_own')|+D] @south_german_gulden_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#swedish_krona
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_swedish_krona_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_swedish_krona_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('swedish_krona_c_global_stokpile')|+D] @swedish_krona_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_swedish_krona_state')|+D] @swedish_krona_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('swedish_krona_c_no_own')|+D] @swedish_krona_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#swiss_franc
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_swiss_franc_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_swiss_franc_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('swiss_franc_c_global_stokpile')|+D] @swiss_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_swiss_franc_state')|+D] @swiss_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('swiss_franc_c_no_own')|+D] @swiss_franc_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#toscane_lira
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_toscane_lira_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_toscane_lira_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('toscane_lira_c_global_stokpile')|+D] @toscane_lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_toscane_lira_state')|+D] @toscane_lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('toscane_lira_c_no_own')|+D] @toscane_lira_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#tunisian_dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_tunisian_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_tunisian_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('tunisian_dinar_c_global_stokpile')|+D] @tunisian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_tunisian_dinar_state')|+D] @tunisian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('tunisian_dinar_c_no_own')|+D] @tunisian_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#tuareg_ouguiya
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_tuareg_ouguiya_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_tuareg_ouguiya_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('tuareg_ouguiya_c_global_stokpile')|+D] @tuareg_ouguiya_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_tuareg_ouguiya_state')|+D] @tuareg_ouguiya_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('tuareg_ouguiya_c_no_own')|+D] @tuareg_ouguiya_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#uni
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_uni_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_uni_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('uni_c_global_stokpile')|+D] @uni_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_uni_state')|+D] @uni_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('uni_c_no_own')|+D] @uni_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#united_states_dollar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_united_states_dollar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_united_states_dollar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('united_states_dollar_c_global_stokpile')|+D] @united_states_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_united_states_dollar_state')|+D] @united_states_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('united_states_dollar_c_no_own')|+D] @united_states_dollar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#uruguayan_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_uruguayan_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_uruguayan_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('uruguayan_peso_c_global_stokpile')|+D] @uruguayan_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_uruguayan_peso_state')|+D] @uruguayan_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('uruguayan_peso_c_no_own')|+D] @uruguayan_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#venezuelan_peso
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_venezuelan_peso_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_venezuelan_peso_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('venezuelan_peso_c_global_stokpile')|+D] @venezuelan_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_venezuelan_peso_state')|+D] @venezuelan_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('venezuelan_peso_c_no_own')|+D] @venezuelan_peso_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#west_african_eco
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_west_african_eco_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_west_african_eco_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('west_african_eco_c_global_stokpile')|+D] @west_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_west_african_eco_state')|+D] @west_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('west_african_eco_c_no_own')|+D] @west_african_eco_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#yen
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_yen_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_yen_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('yen_c_global_stokpile')|+D] @yen_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_yen_state')|+D] @yen_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('yen_c_no_own')|+D] @yen_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#yuan
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_yuan_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_yuan_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('yuan_c_global_stokpile')|+D] @yuan_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_yuan_state')|+D] @yuan_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('yuan_c_no_own')|+D] @yuan_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#yugoslav_dinar
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_yugoslav_dinar_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_yugoslav_dinar_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('yugoslav_dinar_c_global_stokpile')|+D] @yugoslav_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_yugoslav_dinar_state')|+D] @yugoslav_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('yugoslav_dinar_c_no_own')|+D] @yugoslav_dinar_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# 	#zloti
# 	flowcontainer = {
# 		direction = horizontal
# 		margin_top = 1
# 		margin_bottom = 10

# 		visible = "[GetScriptedGui('law_zloti_currency_visible').IsShown( GuiScope.SetRoot(GetPlayer.MakeScope).End)]"

# 		spacing = 20

# 		ef_bp_zloti_c_global_stokpile_piechart = {}

# 		flowcontainer = {
# 			direction = vertical

# 			spacing = 30

# 			parentanchor = vcenter

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "total_currency_product"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('zloti_c_global_stokpile')|+D] @zloti_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }

# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('stockpiling_zloti_state')|+D] @zloti_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}

# 			flowcontainer = {
# 				direction = vertical

# 				spacing = 5

# 				parentanchor = hcenter

# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "currency_no_own"
# 					parentanchor = hcenter
# 					align = hcenter
# 					multiline = yes
# 					maximumsize = { 225 -1 }


# 				}
# 				textbox = {
# 					using = fontsize_large
# 					autoresize = yes
# 					text = "[GetPlayer.MakeScope.ScriptValue('zloti_c_no_own')|+D] @zloti_c!"
# 					parentanchor = hcenter
# 					align = hcenter

# 				}
# 				background = {
# 					using = entry_bg_simple
# 					margin = { 10 10 }
# 				}
# 				minimumsize = { 225 40 }
# 				maximumsize = { 225 -1 }
# 			}
# 		}
# 	}
# }
